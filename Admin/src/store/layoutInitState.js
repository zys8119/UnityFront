/**
 * 布局数据
 * @returns {{footerShow: boolean}}
 */
export default ()=>{
    return {
        // 是否显示页脚
        footerShow:false,
        // 是否tabs
        tabsShow:true,
        // 是否tabs右侧操作按钮
        tabsShowBtn:true,
        // tabs右侧附加操作按钮
        tabsShowBtnArr:null,
    }
}